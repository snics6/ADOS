"""Per-task feature extraction from multimodal_session_v1 JSON.

Two extractors, same JSON:

- ``ados_extract.dynamics`` — head / body motion, speech timing, pairing
- ``ados_extract.windows`` — first vs second half, replies, gestures, text
"""
